<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.helperserver.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.helperserver.bean.User">
        <id column="user_id" property="userId" />
        <result column="username" property="username" />
        <result column="password_hash" property="passwordHash" />
        <result column="email" property="email" />
        <result column="phone" property="phone" />
        <result column="created_at" property="createdAt" />
    </resultMap>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT user_id, username, password_hash, email FROM user WHERE user_id = #{userId}
    </select>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT * FROM user WHERE (email = #{username} OR phone = #{username} OR username = #{username})
    </select>

    <select id="selectByEmail" resultMap="BaseResultMap">
        SELECT * FROM user WHERE email = #{email}
    </select>

    <select id="selectByPhone" resultMap="BaseResultMap">
        SELECT * FROM user WHERE phone = #{phone}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM user
    </select>

    <insert id="insert" parameterType="com.helperserver.bean.User">
        INSERT INTO user (user_id, username, password_hash, email)
        VALUES (#{userId}, #{username}, #{passwordHash}, #{email})
    </insert>

    <update id="update" parameterType="com.helperserver.bean.User">
        UPDATE user
        SET username = #{username},
            password_hash = #{passwordHash},
            email = #{email},
            phone = #{phone},
            created_at = #{createdAt}
        WHERE user_id = #{userId}
    </update>

    <delete id="deleteById">
        DELETE FROM user WHERE user_id = #{userId}
    </delete>
</mapper>