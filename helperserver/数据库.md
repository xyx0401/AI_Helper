## **1. 核心表设计**

### **1.1 用户管理模块**

#### **表：`user`（用户信息）**

| 字段名        | 类型         | 说明                       | 约束                      |
| :------------ | :----------- | :------------------------- | :------------------------ |
| user_id       | VARCHAR(36)  | 用户唯一ID（UUID）         | PRIMARY KEY               |
| username      | VARCHAR(50)  | 用户名（唯一）             | NOT NULL, UNIQUE          |
| password_hash | VARCHAR(255) | 加密后的密码               | NOT NULL                  |
| email         | VARCHAR(100) | 邮箱（唯一）               | NOT NULL, UNIQUE          |
| phone         | VARCHAR(20)  | 手机号                     |                           |
| role          | VARCHAR(20)  | 角色（普通用户、管理员等） | DEFAULT 'user'            |
| created_at    | TIMESTAMP    | 创建时间                   | DEFAULT CURRENT_TIMESTAMP |

#### **表：`user_preference`（用户偏好）**

| 字段名        | 类型        | 说明                     | 约束              |
| :------------ | :---------- | :----------------------- | :---------------- |
| preference_id | VARCHAR(36) | 偏好ID                   | PRIMARY KEY       |
| user_id       | VARCHAR(36) | 关联用户ID               | FOREIGN KEY(user) |
| interests     | TEXT        | 用户兴趣标签（JSON格式） |                   |
| language      | VARCHAR(10) | 用户首选语言             | DEFAULT 'zh-CN'   |

------

### **1.2 会议管理模块**

#### **表：`conference`（会议信息）**

| 字段名        | 类型         | 说明                     | 约束        |
| :------------ | :----------- | :----------------------- | :---------- |
| conference_id | VARCHAR(36)  | 会议唯一ID               | PRIMARY KEY |
| title         | VARCHAR(200) | 会议标题                 | NOT NULL    |
| location      | VARCHAR(255) | 会议地点（经纬度或地址） | NOT NULL    |
| start_time    | TIMESTAMP    | 开始时间                 | NOT NULL    |
| end_time      | TIMESTAMP    | 结束时间                 | NOT NULL    |
| description   | TEXT         | 会议描述                 |             |

#### **表：`agenda`（会议议程）**

| 字段名        | 类型         | 说明           | 约束                    |
| :------------ | :----------- | :------------- | :---------------------- |
| agenda_id     | VARCHAR(36)  | 议程ID         | PRIMARY KEY             |
| conference_id | VARCHAR(36)  | 关联会议ID     | FOREIGN KEY(conference) |
| session_title | VARCHAR(200) | 议程标题       | NOT NULL                |
| speaker_id    | VARCHAR(36)  | 关联演讲嘉宾ID | FOREIGN KEY(speaker)    |
| start_time    | TIMESTAMP    | 议程开始时间   | NOT NULL                |
| end_time      | TIMESTAMP    | 议程结束时间   | NOT NULL                |

#### **表：`speaker`（演讲嘉宾）**

| 字段名     | 类型         | 说明      | 约束        |
| :--------- | :----------- | :-------- | :---------- |
| speaker_id | VARCHAR(36)  | 嘉宾ID    | PRIMARY KEY |
| name       | VARCHAR(100) | 嘉宾姓名  | NOT NULL    |
| title      | VARCHAR(100) | 职位/头衔 |             |
| bio        | TEXT         | 个人简介  |             |

------

### **1.3 问答与推荐模块**

#### **表：`qa_knowledge`（问答知识库）**

| 字段名        | 类型        | 说明                | 约束                    |
| :------------ | :---------- | :------------------ | :---------------------- |
| qa_id         | VARCHAR(36) | 问答ID              | PRIMARY KEY             |
| question      | TEXT        | 用户问题            | NOT NULL                |
| answer        | TEXT        | 答案                | NOT NULL                |
| score         | FLOAT       | 模型匹配得分（0-1） | DEFAULT 0.9             |
| conference_id | VARCHAR(36) | 关联会议ID（可选）  | FOREIGN KEY(conference) |

#### **表：`recommendation`（推荐记录）**

| 字段名            | 类型        | 说明                           | 约束                    |
| :---------------- | :---------- | :----------------------------- | :---------------------- |
| recommendation_id | VARCHAR(36) | 推荐ID                         | PRIMARY KEY             |
| user_id           | VARCHAR(36) | 关联用户ID                     | FOREIGN KEY(user)       |
| conference_id     | VARCHAR(36) | 关联会议ID                     | FOREIGN KEY(conference) |
| agenda_id         | VARCHAR(36) | 关联议程ID                     | FOREIGN KEY(agenda)     |
| reason            | TEXT        | 推荐理由（基于用户兴趣或行为） |                         |

------

### **1.4 实时交互与内容管理**

#### **表：`meeting_summary`（会议总结）**

| 字段名        | 类型        | 说明                   | 约束                    |
| :------------ | :---------- | :--------------------- | :---------------------- |
| summary_id    | VARCHAR(36) | 总结ID                 | PRIMARY KEY             |
| conference_id | VARCHAR(36) | 关联会议ID             | FOREIGN KEY(conference) |
| key_points    | TEXT        | 会议关键点（JSON格式） |                         |
| todo_list     | TEXT        | 待办事项（JSON格式）   |                         |
| generated_by  | VARCHAR(36) | 生成者（AI或用户）     |                         |

#### **表：`resource`（会议资料）**

| 字段名        | 类型         | 说明                  | 约束                    |
| :------------ | :----------- | :-------------------- | :---------------------- |
| resource_id   | VARCHAR(36)  | 资料ID                | PRIMARY KEY             |
| conference_id | VARCHAR(36)  | 关联会议ID            | FOREIGN KEY(conference) |
| agenda_id     | VARCHAR(36)  | 关联议程ID            | FOREIGN KEY(agenda)     |
| file_url      | VARCHAR(255) | 文件下载链接          | NOT NULL                |
| file_type     | VARCHAR(20)  | 文件类型（PPT/PDF等） |                         |

------

### **1.5 数据安全与日志**

#### **表：`access_log`（访问日志）**

| 字段名     | 类型        | 说明                           | 约束                      |
| :--------- | :---------- | :----------------------------- | :------------------------ |
| log_id     | VARCHAR(36) | 日志ID                         | PRIMARY KEY               |
| user_id    | VARCHAR(36) | 关联用户ID                     | FOREIGN KEY(user)         |
| action     | VARCHAR(50) | 操作类型（登录、下载、查询等） | NOT NULL                  |
| timestamp  | TIMESTAMP   | 操作时间                       | DEFAULT CURRENT_TIMESTAMP |
| ip_address | VARCHAR(45) | 用户IP地址                     |                           |

#### **表：`encryption_key`（加密密钥）**

| 字段名        | 类型        | 说明         | 约束              |
| :------------ | :---------- | :----------- | :---------------- |
| key_id        | VARCHAR(36) | 密钥ID       | PRIMARY KEY       |
| user_id       | VARCHAR(36) | 关联用户ID   | FOREIGN KEY(user) |
| encrypted_key | TEXT        | 加密后的密钥 | NOT NULL          |

------

## **2. 扩展表设计（按功能补充）**

### **2.1 地图指引**

#### **表：`location`（会议地点详情）**

| 字段名        | 类型         | 说明       | 约束                    |
| :------------ | :----------- | :--------- | :---------------------- |
| location_id   | VARCHAR(36)  | 地点ID     | PRIMARY KEY             |
| conference_id | VARCHAR(36)  | 关联会议ID | FOREIGN KEY(conference) |
| latitude      | FLOAT        | 纬度       | NOT NULL                |
| longitude     | FLOAT        | 经度       | NOT NULL                |
| address       | VARCHAR(255) | 详细地址   |                         |

------

### **2.2 论坛关注度排行**

#### **表：`forum_topic`（论坛议题热度）**

| 字段名        | 类型         | 说明        | 约束                    |
| :------------ | :----------- | :---------- | :---------------------- |
| topic_id      | VARCHAR(36)  | 议题ID      | PRIMARY KEY             |
| conference_id | VARCHAR(36)  | 关联会议ID  | FOREIGN KEY(conference) |
| title         | VARCHAR(200) | 议题标题    | NOT NULL                |
| view_count    | INT          | 浏览次数    | DEFAULT 0               |
| vote_count    | INT          | 投票/点赞数 | DEFAULT 0               |

------

### **2.3 实时语音与翻译**

#### **表：`speech_transcript`（语音转写记录）**



| 字段名          | 类型        | 说明             | 约束                    |
| :-------------- | :---------- | :--------------- | :---------------------- |
| transcript_id   | VARCHAR(36) | 转写ID           | PRIMARY KEY             |
| conference_id   | VARCHAR(36) | 关联会议ID       | FOREIGN KEY(conference) |
| speaker_id      | VARCHAR(36) | 关联发言人ID     | FOREIGN KEY(speaker)    |
| original_text   | TEXT        | 原始语音转写文本 | NOT NULL                |
| translated_text | TEXT        | 翻译后的文本     |                         |







```sql
-- 用户表
CREATE TABLE user (
    user_id VARCHAR(36) PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    password_hash VARCHAR(255) NOT NULL,
    email VARCHAR(100) NOT NULL UNIQUE,
    phone VARCHAR(20),
    role VARCHAR(20) DEFAULT 'user',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 用户偏好表
CREATE TABLE user_preference (
    preference_id VARCHAR(36) PRIMARY KEY,
    user_id VARCHAR(36) NOT NULL,
    interests TEXT,
    language VARCHAR(10) DEFAULT 'zh-CN',
    FOREIGN KEY (user_id) REFERENCES user(user_id) ON DELETE CASCADE
);

-- 会议表
CREATE TABLE conference (
    conference_id VARCHAR(36) PRIMARY KEY,
    title VARCHAR(200) NOT NULL,
    location VARCHAR(255) NOT NULL,
    start_time TIMESTAMP NOT NULL,
    end_time TIMESTAMP NOT NULL,
    description TEXT
);

-- 演讲嘉宾表
CREATE TABLE speaker (
    speaker_id VARCHAR(36) PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    title VARCHAR(100),
    bio TEXT
);

-- 会议议程表
CREATE TABLE agenda (
    agenda_id VARCHAR(36) PRIMARY KEY,
    conference_id VARCHAR(36) NOT NULL,
    session_title VARCHAR(200) NOT NULL,
    speaker_id VARCHAR(36),
    start_time TIMESTAMP NOT NULL,
    end_time TIMESTAMP NOT NULL,
    FOREIGN KEY (conference_id) REFERENCES conference(conference_id) ON DELETE CASCADE,
    FOREIGN KEY (speaker_id) REFERENCES speaker(speaker_id) ON DELETE SET NULL
);

-- 问答知识库表
CREATE TABLE qa_knowledge (
    qa_id VARCHAR(36) PRIMARY KEY,
    question TEXT NOT NULL,
    answer TEXT NOT NULL,
    score FLOAT DEFAULT 0.9,
    conference_id VARCHAR(36),
    FOREIGN KEY (conference_id) REFERENCES conference(conference_id) ON DELETE CASCADE
);

-- 推荐记录表
CREATE TABLE recommendation (
    recommendation_id VARCHAR(36) PRIMARY KEY,
    user_id VARCHAR(36) NOT NULL,
    conference_id VARCHAR(36) NOT NULL,
    agenda_id VARCHAR(36),
    reason TEXT,
    FOREIGN KEY (user_id) REFERENCES user(user_id) ON DELETE CASCADE,
    FOREIGN KEY (conference_id) REFERENCES conference(conference_id) ON DELETE CASCADE,
    FOREIGN KEY (agenda_id) REFERENCES agenda(agenda_id) ON DELETE SET NULL
);

-- 会议总结表
CREATE TABLE meeting_summary (
    summary_id VARCHAR(36) PRIMARY KEY,
    conference_id VARCHAR(36) NOT NULL,
    key_points TEXT,
    todo_list TEXT,
    generated_by VARCHAR(36),
    FOREIGN KEY (conference_id) REFERENCES conference(conference_id) ON DELETE CASCADE
);

-- 会议资料表
CREATE TABLE resource (
    resource_id VARCHAR(36) PRIMARY KEY,
    conference_id VARCHAR(36) NOT NULL,
    agenda_id VARCHAR(36),
    file_url VARCHAR(255) NOT NULL,
    file_type VARCHAR(20),
    FOREIGN KEY (conference_id) REFERENCES conference(conference_id) ON DELETE CASCADE,
    FOREIGN KEY (agenda_id) REFERENCES agenda(agenda_id) ON DELETE SET NULL
);

-- 访问日志表
CREATE TABLE access_log (
    log_id VARCHAR(36) PRIMARY KEY,
    user_id VARCHAR(36) NOT NULL,
    action VARCHAR(50) NOT NULL,
    timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    ip_address VARCHAR(45),
    FOREIGN KEY (user_id) REFERENCES user(user_id) ON DELETE CASCADE
);

-- 加密密钥表
CREATE TABLE encryption_key (
    key_id VARCHAR(36) PRIMARY KEY,
    user_id VARCHAR(36) NOT NULL,
    encrypted_key TEXT NOT NULL,
    FOREIGN KEY (user_id) REFERENCES user(user_id) ON DELETE CASCADE
);

-- 会议地点详情表
CREATE TABLE location (
    location_id VARCHAR(36) PRIMARY KEY,
    conference_id VARCHAR(36) NOT NULL,
    latitude FLOAT NOT NULL,
    longitude FLOAT NOT NULL,
    address VARCHAR(255),
    FOREIGN KEY (conference_id) REFERENCES conference(conference_id) ON DELETE CASCADE
);

-- 论坛议题热度表
CREATE TABLE forum_topic (
    topic_id VARCHAR(36) PRIMARY KEY,
    conference_id VARCHAR(36) NOT NULL,
    title VARCHAR(200) NOT NULL,
    view_count INT DEFAULT 0,
    vote_count INT DEFAULT 0,
    FOREIGN KEY (conference_id) REFERENCES conference(conference_id) ON DELETE CASCADE
);

-- 语音转写记录表
CREATE TABLE speech_transcript (
    transcript_id VARCHAR(36) PRIMARY KEY,
    conference_id VARCHAR(36) NOT NULL,
    speaker_id VARCHAR(36),
    original_text TEXT NOT NULL,
    translated_text TEXT,
    FOREIGN KEY (conference_id) REFERENCES conference(conference_id) ON DELETE CASCADE,
    FOREIGN KEY (speaker_id) REFERENCES speaker(speaker_id) ON DELETE SET NULL
);

-- 创建用户信息表
CREATE TABLE IF NOT EXISTS user_info (
    user_id VARCHAR(36) PRIMARY KEY,
    username VARCHAR(50) NOT NULL,
    email VARCHAR(100),
    phone VARCHAR(20),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_username (username),
    INDEX idx_email (email)
);

-- 创建资源下载表
CREATE TABLE IF NOT EXISTS resource_download (
    download_id VARCHAR(36) PRIMARY KEY,
    resource_id VARCHAR(36) NOT NULL,
    user_id VARCHAR(36) NOT NULL,
    file_name VARCHAR(255) NOT NULL,
    file_type VARCHAR(100),
    file_size BIGINT,
    download_url VARCHAR(255) NOT NULL,
    download_count INT DEFAULT 0,
    last_download_time TIMESTAMP,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES user_info(user_id),
    INDEX idx_resource_id (resource_id),
    INDEX idx_user_id (user_id)
);

-- 创建索引
CREATE UNIQUE INDEX idx_user_username ON user(username);
CREATE UNIQUE INDEX idx_user_email ON user(email);
CREATE INDEX idx_conference_start_time ON conference(start_time);
CREATE FULLTEXT INDEX idx_qa_question ON qa_knowledge(question);
```