<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.helperserver.mapper.ForumTopicMapper">
    <!-- 基础字段映射 -->
    <resultMap id="BaseResultMap" type="com.helperserver.bean.ForumTopic">
        <id column="topic_id" property="topicId" />
        <result column="conference_id" property="conferenceId" />
        <result column="title" property="title" />
        <result column="content" property="content" />
        <result column="view_count" property="viewCount" />
        <result column="vote_count" property="voteCount" />
        <result column="comment_count" property="commentCount" />
        <result column="favorite_count" property="favoriteCount" />
        <result column="hot_score" property="hotScore" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 获取指定时间范围内的热门话题 -->
    <select id="selectHotTopics" resultMap="BaseResultMap">
        SELECT *
        FROM forum_topic
        WHERE 1=1
        <if test="startTime != null">
            AND create_time >= #{startTime}
        </if>
        <if test="endTime != null">
            AND create_time &lt;= #{endTime}
        </if>
        ORDER BY hot_score DESC
        LIMIT #{limit}
    </select>

    <!-- 根据ID查询话题 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT *
        FROM forum_topic
        WHERE topic_id = #{topicId}
    </select>

    <!-- 更新话题信息 -->
    <update id="update">
        UPDATE forum_topic
        SET conference_id = #{conferenceId},
            title = #{title},
            content = #{content},
            view_count = #{viewCount},
            vote_count = #{voteCount},
            comment_count = #{commentCount},
            favorite_count = #{favoriteCount},
            hot_score = #{hotScore},
            update_time = #{updateTime}
        WHERE topic_id = #{topicId}
    </update>

    <!-- 增加浏览次数 -->
    <update id="incrementViewCount">
        UPDATE forum_topic
        SET view_count = view_count + 1,
            update_time = NOW()
        WHERE topic_id = #{topicId}
    </update>

    <!-- 增加点赞数 -->
    <update id="incrementVoteCount">
        UPDATE forum_topic
        SET vote_count = vote_count + 1,
            update_time = NOW()
        WHERE topic_id = #{topicId}
    </update>

    <!-- 增加评论数 -->
    <update id="incrementCommentCount">
        UPDATE forum_topic
        SET comment_count = comment_count + 1,
            update_time = NOW()
        WHERE topic_id = #{topicId}
    </update>

    <!-- 增加收藏数 -->
    <update id="incrementFavoriteCount">
        UPDATE forum_topic
        SET favorite_count = favorite_count + 1,
            update_time = NOW()
        WHERE topic_id = #{topicId}
    </update>
</mapper>